# Default values for todo.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 2
image:
  repository: harbor-01a.corp.local/sys-2b0109it/node-todo
  tag: latest
  pullPolicy: Always
service:
  name: http
  type: ClusterIP
  externalPort: 80
  internalPort: 8080
resources:
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi
ingress:
  host: todos.local
  # Enable automatic TLS with Let's Encrypt
  tls: true

mongodb:
  image:
    registry: harbor-01a.corp.local
    repository: demo/bitnami/mongodb
    tag: 4.2-centos-7
  # Disable authentication TODO(change) only for demo purposes
  usePassword: false
